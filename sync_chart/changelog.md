# sync_chart 变更记录

本文件记录 `sync_chart` 目录下脚本与配置的变更历史；新版本追加在顶部。

与 **PAT / Git 预同步 / oauth2 HTTPS** 等跨目录改动，亦可能出现在仓库根 **`README.md`** 与 **`compare_chart/changelog.md`** 的对应条目中。

---

## [未发布]

<!-- 下一次发布或打 tag 前在此累积条目 -->

### 文档

- 新增 **skills.md**：面向 Agent 的 **`!sync_chart`** 触发方式、与 `sync_chart.py` 参数映射、默认 **`config.yaml`**（脚本所在目录，与 `compare_chart` 的「当前工作目录」语义不同）、**`--skip-sync`** / **`--token-source`**、凭证与安全约束、配置字段说明等。
- 新增 **changelog.md**（本文件）：独立记录本目录变更历史。
